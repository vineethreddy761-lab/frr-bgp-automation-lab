---
- name: Lab Configuration Management
  hosts: local
  tasks:
    - name: Ensure lab config directory exists
      file:
        path: /tmp/lab-configs
        state: directory
        mode: '0755'
    - name: Display status message
      debug:
        msg: "Ansible configuration management executed successfully in local lab!"
